export enum UserVerifyStatus {
  Unverifyed,
  Verifyed,
  Banned
}

export enum TokenTypes {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum PostTypes {
  Post,
  RePost
}

export enum MediaTypes {
  Image,
  Video
}
