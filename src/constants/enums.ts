export enum UserVerifyStatus {
  Unverifyed,
  Verifyed,
  Banned
}

export enum TokenTypes {
  AccessToken,
  RefreshToken,
  ResetPasswordToken,
  EmailVerifyToken
}

export enum PostTypes {
  Post,
  RePost
}

export enum MediaTypes {
  Image,
  Video
}
